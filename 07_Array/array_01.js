// ARRAY:-
// Array is an object in java script which is store multiple type data
// Array is a non-primitive type data

const myArr = [0,1,2,3,4,5,]
console.log(myArr[2]);

const myHero = ["Shaktiman", "NagRaj", "IronMan", "Thor"]
console.log(myHero[0]);

// Array size is dynamic, we can store no of elements and js engnie handle it's automatically 

const myArr2 = new Array(0,1,2,3,4,5,6,7);
console.log(myArr2);
const newArr = myArr2;
console.log(`New Array ${newArr}`);

// Array is reusable
// Array is a block of memory


// Array elements should be separated by comma(,)

// If you try access the index greater then the array length. we will get undefind

// Array Index = length - 1